<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <SelectDialog
        id="idSelectSupplierDialog"
        title="{i18n>Suppliers}"
        multiSelect="true"
        items="{
            path: '/Suppliers',
            sorter: { path: 'Name' }
        }"
        search="onSelectSuppliersDialogSearch"
        confirm="onSelectSuppliersDialogConfirm"
    >
        <StandardListItem
            title="{Name}"
            description="{Country}"
        />
    </SelectDialog>
</core:FragmentDefinition>
